--- src/symbol.inc	2020-08-25 08:27:23.000000000 -0700
+++ src/symbol.inc.new	2025-05-27 13:15:04.984375967 -0700
@@ -575,8 +575,8 @@
 !
 ! Macros for GNU version specific code
 !
-#ifdef __GNUC__
-#if __GNUC__ > 7
-#pragma GCC error "Compilation of VASP with gfortran is currently supported only up to gfortran version 7 ... sorry"
-#endif
-#endif
+! #ifdef __GNUC__
+! #if __GNUC__ > 7
+! #pragma GCC error "Compilation of VASP with gfortran is currently supported only up to gfortran version 7 ... sorry"
+! #endif
+! #endif
